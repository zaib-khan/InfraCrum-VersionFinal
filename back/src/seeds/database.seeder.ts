
export async function seedDatabase () {
  // const enterpriseRepo = datasource.getRepository(EnterpriseModel);
  // // const userRepo = datasource.getRepository(UserModel);

  // await datasource.query('SET foreign_key_checks = 0');

  // await enterpriseRepo.clear();
  // // await userRepo.clear();

  // const enterprise = enterpriseRepo.create({
  //   id: 123,
  //   name: 'Fake enterprise',
  //   img: 'https://upload.wikimedia.org/wikipedia/commons/thumb/4/4d/Cat_November_2010-1a.jpg/1200px-Cat_November_2010-1a.jpg',
  // });

  // const user = userRepo.create({
  //   name: 'Amaury',
  // });

  // await userRepo.save(user);

  // enterprise.users = [user];

  // await enterpriseRepo.save(enterprise);

  // await datasource.query('SET foreign_key_checks = 1');
}
